import { createPinia, setActivePinia } from 'pinia'
import { beforeEach, describe, it } from 'vitest'

describe('Timer Store', () => {
  beforeEach(() => {
    setActivePinia(createPinia())
  })
  it('create WebWorker', () => {})
  it('start timer', () => {})
  it('stop timer', () => {})
  it('reset timer', () => {})
  it('WebWorker is terminated', () => {})
  it('Is WebWorker mounting with store')
})
