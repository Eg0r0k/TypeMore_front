type Stats = {
  wpm: number
  wpmRaw: number
  acc: number
  correctChars: number
  missedChars: number
  extraChars: number
  spaces: number
  allChars: number
}

export const useStats = () => {
  const ret = {}
  const getStats = () => {
    return
  }
  const setEnd = ()=>{

  }
  const setStart = ()=>{
  
  }
}
