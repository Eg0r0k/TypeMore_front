export { default as RoomConfig } from './ui.vue'
/** The room's name + visibility — rendered beside the code, not with the run config. */
export { default as RoomIdentity } from './parts/Identity.vue'
